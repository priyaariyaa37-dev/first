const prompt = require('prompt-sync') ();
let P= Number(prompt ("Enter the value of P"));
let R= Number(prompt ("Enter the value of R"));
let T= Number(prompt ("Enter the value of T"));
let SI= (P*R*T)/100;
console.log(SI)